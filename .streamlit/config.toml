[server]
headless = true
# Disable file watcher in production (saves CPU, no live-reload needed)
fileWatcherType = "none"
enableCORS = false

[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#FF6B35"
backgroundColor = "#0E1117"
secondaryBackgroundColor = "#1A1F2E"
textColor = "#FAFAFA"
